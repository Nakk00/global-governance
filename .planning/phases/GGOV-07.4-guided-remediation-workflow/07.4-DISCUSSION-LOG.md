# Phase 07.4: Guided Remediation Workflow - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-05-16
**Phase:** 07.4-guided-remediation-workflow
**Areas discussed:** Queue scope and data source, Triage vocabulary, Destination surface and links, Workbench placement, Testing and module shape

---

## Queue scope and data source

| Option | Description | Selected |
|--------|-------------|----------|
| Derived from active run detail | Reuse `ValidationRunDetail.results` and keep the queue read-only. | ✓ |
| Persisted remediation store | Add a new backend data layer for remediation items. | |
| Cross-run backlog | Aggregate every failed result across the dashboard. | |

**User's choice:** Auto-selected recommended default.
**Notes:** The workbench already owns the run-detail payload, so a derived queue keeps the surface deterministic and avoids a new persistence seam.

---

## Triage vocabulary

| Option | Description | Selected |
|--------|-------------|----------|
| Local outcome mapper | Map each outcome to a stable likely-cause label and review status. | ✓ |
| Extend source labels | Reuse the source inventory validation pills for remediation. | |
| Mutable triage state | Let maintainers change item status in 07.4. | |

**User's choice:** Auto-selected recommended default.
**Notes:** The queue needs its own read-only triage vocabulary because 07.4 is review-only and source inventory labels are already serving a different surface.

---

## Destination surface and links

| Option | Description | Selected |
|--------|-------------|----------|
| Source detail for weak support, result detail for the rest | Use the existing source detail route when source context is strong, otherwise open the workbench's result overlay. | ✓ |
| Source detail only | Route every item to source detail, even when the result itself is the better review surface. | |
| New remediation route | Add a dedicated remediation page or route section. | |

**User's choice:** Auto-selected recommended default.
**Notes:** The existing private source detail page and the validation result overlay already cover the needed follow-up surfaces, so no new route is required.

---

## Workbench placement

| Option | Description | Selected |
|--------|-------------|----------|
| Inline section in validation workbench | Place the queue below the evidence table and above run history. | ✓ |
| Separate remediation page | Split follow-up into a new page. | |
| Right-rail widget | Add a compact side panel next to the run detail. | |

**User's choice:** Auto-selected recommended default.
**Notes:** Keeping the queue in the workbench preserves the evidence-first flow and makes the remediation view feel like a follow-up to the result table instead of a second console.

---

## Testing and module shape

| Option | Description | Selected |
|--------|-------------|----------|
| Pure local mapper with Vitest / RTL coverage | Keep the queue derivation testable in the validation feature folder. | ✓ |
| Add Playwright coverage first | Prove the queue through browser tests before unit tests. | |
| Inline everything in the workbench file | Add the logic directly to `ValidationWorkbench.tsx` without a helper seam. | |

**User's choice:** Auto-selected recommended default.
**Notes:** The queue is derived UI logic, so the fastest and clearest proof is a pure mapper plus co-located frontend tests.

---

## the agent's Discretion

- Exact file names for the queue component and mapper can be finalized in
  planning as long as the workbench remains the only entry point.
- The exact visual treatment can stay flexible between a compact table and a
  card list, provided the ordering and read-only behavior stay stable.

## Deferred Ideas

- Helper actions such as open source, inspect citations, rerun validation,
  queue re-ingest, mark expected refusal, and AI suggestions were noted for
  later phases.
- Mutable close / resolve state was explicitly deferred to future remediation
  phases.
