---
phase: 05-admin-ux-polish-for-maintainers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/sources/dtos.py
  - backend/sources/services.py
  - backend/sources/views.py
  - backend/validation/services.py
  - backend/validation/views.py
  - src/lib/maintainer/api.ts
  - src/lib/maintainer/api.test.ts
  - backend/tests/test_admin_stewardship.py
  - backend/tests/test_admin_validation.py
autonomous: true
requirements:
  - ADMIN-01
user_setup: []
must_haves:
  truths:
    - "D-09/D-10/D-11/ADMIN-01: The maintainer dashboard contract exposes richer monitoring data for overview, Audit Trail, and Chatbot Trust surfaces instead of relying on browser-only derivations."
    - "D-06/ADMIN-01: The private maintainer auth boundary still gates all dashboard and monitoring reads."
    - "D-09/D-11: The backend remains the canonical place for monitoring aggregation and dashboard shaping."
  artifacts:
    - path: backend/sources/dtos.py
      provides: richer maintainer dashboard DTO fields and monitoring aggregates
      min_lines: 120
      contains: StewardshipDashboardDto
    - path: backend/sources/services.py
      provides: backend shaping helpers for dashboard, audit, and trust monitoring data
      min_lines: 80
      contains: get_stewardship_dashboard
    - path: src/lib/maintainer/api.ts
      provides: matching frontend contract types for the richer dashboard data
      min_lines: 180
      contains: StewardshipDashboard
    - path: backend/tests/test_admin_stewardship.py
      provides: backend coverage for the richer dashboard contract and read boundary
      min_lines: 120
    - path: backend/tests/test_admin_validation.py
      provides: validation-side coverage for trust-related summary behavior
      min_lines: 100
  key_links:
    - from: backend/sources/views.py
      to: backend/sources/services.py
      via: auth-gated dashboard reads
      pattern: get_stewardship_dashboard
    - from: backend/sources/services.py
      to: backend/sources/dtos.py
      via: dashboard shaping contract
      pattern: StewardshipDashboardDto
    - from: backend/validation/views.py
      to: backend/validation/services.py
      via: validation summary and trust data reads
      pattern: list_validation_runs
    - from: src/lib/maintainer/api.ts
      to: backend/sources/views.py
      via: frontend contract mirror for the maintainer dashboard payload
      pattern: fetchStewardshipDashboard
---

<objective>
Extend the private maintainer monitoring contract so the backend becomes the canonical source for richer overview, audit, and chatbot-trust signals.

Purpose: Give Phase 5 a stable server-shaped monitoring model before the control-center UI is rebuilt, so the new sections can stay presentation-focused and the admin boundary remains private.
Output: A richer maintainer dashboard DTO/service layer, mirrored frontend contract types, and backend/API boundary tests that prove the new monitoring data is available behind the admin gate.
</objective>

<execution_context>
@C:/Users/Nakko/Desktop/VSCode-ProjectFiles/Global-Governance/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Nakko/Desktop/VSCode-ProjectFiles/Global-Governance/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-ux-polish-for-maintainers/05-CONTEXT.md
@.planning/phases/05-admin-ux-polish-for-maintainers/05-RESEARCH.md
@src/lib/maintainer/api.ts
@backend/sources/dtos.py
@backend/sources/services.py
@backend/sources/views.py
@backend/validation/services.py
@backend/validation/views.py
@backend/tests/test_admin_stewardship.py
@backend/tests/test_admin_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shape the richer maintainer monitoring payload in Django</name>
  <files>backend/sources/dtos.py, backend/sources/services.py, backend/sources/views.py, backend/validation/services.py, backend/validation/views.py</files>
  <read_first>backend/sources/dtos.py, backend/sources/services.py, backend/sources/views.py, backend/validation/services.py, backend/validation/views.py, .planning/phases/05-admin-ux-polish-for-maintainers/05-CONTEXT.md, .planning/phases/05-admin-ux-polish-for-maintainers/05-RESEARCH.md</read_first>
  <action>Extend the maintainer dashboard contract so the backend can supply richer monitoring data for the Phase 5 control center. Keep the current private admin gate intact, but add the server-shaped aggregates needed for the new overview, Audit Trail, and Chatbot Trust surfaces. Prefer extending the existing dashboard DTO and service shaping layers instead of inventing a parallel admin contract or moving monitoring logic into the browser.</action>
  <verify>pnpm backend:test</verify>
  <done>The backend now returns richer admin monitoring data behind the existing maintainer boundary, and the admin read-path tests prove the new payload shape and gate behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the richer dashboard contract in the frontend API boundary</name>
  <files>src/lib/maintainer/api.ts, src/lib/maintainer/api.test.ts</files>
  <read_first>src/lib/maintainer/api.ts, src/lib/maintainer/api.test.ts, .planning/phases/05-admin-ux-polish-for-maintainers/05-RESEARCH.md, backend/sources/dtos.py</read_first>
  <action>Update the frontend maintainer API types and parsing helpers so they match the richer backend monitoring payload. Keep the fetch boundary typed, preserve the existing auth-failure clearing behavior, and make sure the new contract can be consumed by the dashboard shell without introducing browser-only monitoring logic.</action>
  <verify>pnpm exec vitest run src/lib/maintainer/api.test.ts</verify>
  <done>The frontend contract mirrors the richer backend payload and still clears auth/session failures the same way as before.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the richer contract with focused backend and API regression tests</name>
  <files>backend/tests/test_admin_stewardship.py, backend/tests/test_admin_validation.py, src/lib/maintainer/api.test.ts</files>
  <read_first>backend/tests/test_admin_stewardship.py, backend/tests/test_admin_validation.py, src/lib/maintainer/api.test.ts, .planning/phases/05-admin-ux-polish-for-maintainers/05-RESEARCH.md</read_first>
  <action>Expand the contract and read-path tests so the phase has proof that the maintainer dashboard payload, validation summary data, and private auth gate still work after the richer monitoring fields are added. Keep the assertions focused on the new summary shape and the unchanged private boundary, not on the later UI shell redesign.</action>
  <verify>pnpm backend:test && pnpm exec vitest run src/lib/maintainer/api.test.ts</verify>
  <done>The richer monitoring contract is covered by backend and API regression tests before the control-center UI changes begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> API | The browser can only read maintainer monitoring data through the auth-gated admin endpoints. |
| API -> repository/service layer | Monitoring aggregates must be shaped server-side so the client does not invent its own truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | E | backend admin endpoints | mitigate | Keep the existing admin auth gate and test the private read-paths explicitly. |
| T-05-02 | T | dashboard DTO shaping | mitigate | Build the richer monitoring payload in services/dtos and verify it through backend tests. |
| T-05-03 | I | maintainer API boundary | mitigate | Mirror the backend contract in `src/lib/maintainer/api.ts` so the browser only consumes canonical fields. |
</threat_model>

<verification>
Overall phase checks:
- `pnpm backend:test` passes after the backend monitoring contract changes
- `pnpm exec vitest run src/lib/maintainer/api.test.ts` passes after the frontend contract mirror is updated
- The richer payload can be fetched through the existing private maintainer admin route without weakening the auth boundary
</verification>

<success_criteria>
The maintainer dashboard has a richer server-shaped monitoring contract that can support the new overview, Audit Trail, and Chatbot Trust sections without pushing monitoring logic into browser-only state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-ux-polish-for-maintainers/05-01-SUMMARY.md`
</output>
