---
phase: 03-maintainer-readiness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx
  - src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx
  - tests/e2e/maintainer-readiness.smoke.spec.ts
autonomous: true
requirements:
  - READY-01
  - READY-02
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: The maintainer landing view reads as a health-summary-first overview with workflow cards for Sources, Validation, and Audit/Operations instead of a blockers-first wall."
    - "D-05/D-06/D-07/READY-01: Each workflow card opens a filtered drill-down that prioritizes problems plus recent inline context, so maintainers can see blockers and next actions immediately."
    - "D-08/D-09/D-10/D-11/READY-02: Selecting a readiness finding takes the maintainer to source detail first, where the current blocking issue is prominent and validation evidence is summarized inline with a deeper link."
    - "D-27: Browser smoke coverage proves the overview-to-source-detail readiness journey without expanding into a broad Playwright matrix."
  artifacts:
    - path: src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx
      provides: readiness-first overview, filtered drill-down routing, source-detail blocker summary, inline validation evidence
      min_lines: 300
      contains: Maintainer overview
    - path: src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx
      provides: frontend integration coverage for readiness cards, drill-down routing, and source-detail evidence summary
      min_lines: 200
    - path: tests/e2e/maintainer-readiness.smoke.spec.ts
      provides: minimal browser proof for overview to source-detail journey
      min_lines: 40
      contains: "@smoke"
  key_links:
    - from: src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx
      to: /maintainer/sources
      via: filtered readiness drill-down navigation
      pattern: maintainer/sources
    - from: src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx
      to: fetchSourceDetail
      via: source-first drill-down data load
      pattern: fetchSourceDetail
    - from: tests/e2e/maintainer-readiness.smoke.spec.ts
      to: src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx
      via: smoke journey assertions
---

<objective>
Reshape the private maintainer information architecture around readiness-first workflows using the existing private SPA shell and current admin contracts.

Purpose: Deliver the clearest maintainer-facing behavior change in the phase first, so blockers, next actions, and source-first investigation become obvious before any extraction or backend seam work.
Output: A health-summary-first overview, filtered drill-down navigation, source-detail-first readiness investigation, inline validation evidence, and minimal browser proof for the key journey.
</objective>

<execution_context>
@C:/Users/Nakko/Desktop/VSCode-ProjectFiles/Global-Governance/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Nakko/Desktop/VSCode-ProjectFiles/Global-Governance/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-maintainer-readiness-hardening/03-CONTEXT.md
@.planning/phases/03-maintainer-readiness-hardening/03-RESEARCH.md
@src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx
@src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx
@src/lib/maintainer/api.ts
@backend/tests/test_admin_stewardship.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the overview around workflow health cards and filtered presets</name>
  <files>src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx</files>
  <read_first>src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx, src/lib/maintainer/api.ts, .planning/phases/03-maintainer-readiness-hardening/03-CONTEXT.md</read_first>
  <action>Extend the maintainer route model so the private shell can preserve a readiness preset for Overview, Sources, Validation, and Operations without introducing React Router. Update the overview page to render three primary workflow cards named exactly `Sources`, `Validation`, and `Audit/Operations`. Each card must display a status signal plus at least one count from existing DTO data: source totals or partial-source totals for Sources, recent run status/counts for Validation, and recent event visibility for Audit/Operations. Replace any generic section-jump behavior with filtered drill-down actions that push a concrete URL state for the target section and keep recent context inline within each workflow card instead of a detached activity strip.</action>
  <verify>pnpm exec vitest run src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx</verify>
  <acceptance_criteria>
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx contains the exact strings "Sources", "Validation", and "Audit/Operations"
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx contains route handling for a preserved readiness preset or filter state, not only the base section names
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx does not rely on a new router package import
  </acceptance_criteria>
  <done>The overview behaves as a workflow-health hub with concrete filtered drill-down entry points and no router rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Make source detail the first investigative stop with inline validation evidence</name>
  <files>src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx</files>
  <read_first>src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx, src/lib/maintainer/api.ts, .planning/phases/03-maintainer-readiness-hardening/03-CONTEXT.md, .planning/phases/03-maintainer-readiness-hardening/03-RESEARCH.md</read_first>
  <action>Add a prominent source-detail summary section that leads with the current blocking issue before broader metadata. Derive the blocking issue from lifecycle state, `partialData`, latest ingest status, or missing validation signals already present in the current DTOs. Make the next easiest action a validation follow-up by rendering an inline validation evidence summary above the detailed history tabs, including the latest outcome, a short explanation, and a direct action that opens the validation workbench or a run detail view without losing the source context. Keep the original sections available as supporting structure, but make the readiness investigation path primary.</action>
  <verify>pnpm exec vitest run src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx</verify>
  <acceptance_criteria>
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx contains a source-detail section whose heading or body explicitly references a blocking issue or readiness blocker
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx contains inline validation evidence content in the source-detail render path before the history tabs
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx still renders the existing supporting sections for overview, chunks, citations, or history rather than removing them entirely
  </acceptance_criteria>
  <done>Maintainers can move from a readiness finding to source detail first and inspect validation context inline without a generic detour.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused frontend and browser regression coverage for the readiness journey</name>
  <files>src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx, tests/e2e/maintainer-readiness.smoke.spec.ts</files>
  <read_first>src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx, tests/e2e, AGENTS.md, src/components/modules/MaintainerDashboard/MaintainerDashboard.tsx</read_first>
  <action>Extend the existing Vitest suite to assert the renamed workflow cards, filtered drill-down behavior, readiness-first source-detail summary, and inline validation evidence summary. Add one checked-in Playwright smoke spec at `tests/e2e/maintainer-readiness.smoke.spec.ts` tagged `@smoke` that covers the high-value browser path from overview -> filtered readiness action -> source detail, using mocked UI-state coverage only. Keep the Playwright scope narrow: do not create a broad matrix for every maintainer state.</action>
  <verify>pnpm test:unit && pnpm test:e2e</verify>
  <acceptance_criteria>
    - src/components/modules/MaintainerDashboard/MaintainerDashboard.test.tsx contains assertions for the exact workflow card labels and the source-detail readiness summary
    - tests/e2e/maintainer-readiness.smoke.spec.ts exists and contains "@smoke"
    - tests/e2e/maintainer-readiness.smoke.spec.ts asserts navigation from the overview into a source-detail readiness path
  </acceptance_criteria>
  <done>The readiness-first workflow is protected by fast frontend integration coverage plus one browser smoke journey.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:unit` passes
- [ ] `pnpm test:e2e` passes
- [ ] The overview presents workflow-health cards and at least one filtered drill-down per card
- [ ] Source detail leads with a blocker summary and inline validation evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No new router, global store, or backend contract rewrite introduced
- Maintainer can understand blockers and next actions from the overview, then move into source detail first for investigation
  </success_criteria>

<output>
After completion, create `.planning/phases/03-maintainer-readiness-hardening/03-01-SUMMARY.md`
</output>
