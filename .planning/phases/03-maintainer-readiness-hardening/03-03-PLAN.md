---
phase: 03-maintainer-readiness-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/sources/services.py
  - backend/sources/repository.py
  - backend/sources/views.py
  - backend/validation/services.py
  - backend/validation/views.py
  - backend/tests/test_admin_stewardship.py
  - backend/tests/test_admin_validation.py
autonomous: true
requirements:
  - READY-02
  - READY-03
user_setup: []
must_haves:
  truths:
    - "D-21/D-22/D-23/D-24: Readiness-oriented stewardship shaping moves into an explicit service layer while repositories remain focused on retrieval, persistence, and mutation rules."
    - "D-25/D-26: Confidence for the refactor comes from frontend integration plus Django API integration seams, not from one layer alone or a broad browser matrix."
    - "READY-02/READY-03: The backend contracts that support source detail, validation evidence, and maintainer workflows remain safe to navigate and safer to extend after the seam split."
  artifacts:
    - path: backend/sources/services.py
      provides: stewardship readiness services above repository access
      min_lines: 60
      contains: get_stewardship_dashboard
    - path: backend/validation/services.py
      provides: validation service seam for list/detail/launch flows
      min_lines: 40
    - path: backend/sources/views.py
      provides: thin views delegating to services instead of repository orchestration
      min_lines: 80
    - path: backend/validation/views.py
      provides: thin validation views delegating to services
      min_lines: 40
    - path: backend/tests/test_admin_stewardship.py
      provides: integration coverage for stewardship contracts after service extraction
      min_lines: 150
    - path: backend/tests/test_admin_validation.py
      provides: integration coverage for validation contracts after service extraction
      min_lines: 120
  key_links:
    - from: backend/sources/views.py
      to: backend/sources/services.py
      via: dashboard, source detail, inspection, and mutation service delegation
    - from: backend/validation/views.py
      to: backend/validation/services.py
      via: validation set/list/detail/launch delegation
    - from: backend/sources/services.py
      to: backend/sources/repository.py
      via: narrowed repository calls for snapshots, detail retrieval, and mutations
---

<objective>
Strengthen the backend stewardship and validation seams so maintainer-facing readiness contracts are easier to reason about and safer to extend without changing the protected boundary shape.

Purpose: Reduce brownfield fragility on the server side by separating readiness-oriented orchestration from raw repository access and backing that split with targeted Django integration tests.
Output: New service-layer modules for sources and validation, thinner admin views, narrower repository ownership, and updated backend tests that protect the source-detail and validation evidence flows.
</objective>

<execution_context>
@C:/Users/Nakko/Desktop/VSCode-ProjectFiles/Global-Governance/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Nakko/Desktop/VSCode-ProjectFiles/Global-Governance/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-maintainer-readiness-hardening/03-CONTEXT.md
@.planning/phases/03-maintainer-readiness-hardening/03-RESEARCH.md
@backend/sources/repository.py
@backend/sources/views.py
@backend/sources/dtos.py
@backend/validation/repository.py
@backend/validation/views.py
@backend/validation/dtos.py
@backend/tests/test_admin_stewardship.py
@backend/tests/test_admin_validation.py
@backend/tests/test_admin_auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce source and validation service layers that own readiness-oriented orchestration</name>
  <files>backend/sources/services.py, backend/validation/services.py, backend/sources/repository.py, backend/validation/repository.py</files>
  <read_first>backend/sources/repository.py, backend/validation/repository.py, backend/sources/dtos.py, backend/validation/dtos.py, .planning/phases/03-maintainer-readiness-hardening/03-CONTEXT.md</read_first>
  <action>Create `backend/sources/services.py` and move stewardship dashboard composition, source-detail readiness shaping, and inspection/readiness orchestration into explicit service functions that preserve the existing DTO envelopes. Create `backend/validation/services.py` that centralizes list/detail/launch orchestration and fallback handling for validation runs. Leave the repositories responsible for raw retrieval, mutation rules, and persistence details. Do not change public endpoint paths or envelope helpers in this plan; the goal is seam clarity, not a contract rewrite.</action>
  <verify>pnpm backend:test</verify>
  <acceptance_criteria>
    - backend/sources/services.py exists and contains a service entrypoint for stewardship dashboard retrieval
    - backend/validation/services.py exists and contains service entrypoints for validation list/detail/launch flows
    - backend/sources/repository.py no longer serves as the only place where readiness-oriented dashboard shaping lives
    - backend/validation/repository.py still owns persistence/fallback mechanics but is not the only orchestration boundary exposed to views
  </acceptance_criteria>
  <done>Backend readiness orchestration has an explicit home above the repositories.</done>
</task>

<task type="auto">
  <name>Task 2: Thin the admin views so they delegate to services and keep safe envelopes unchanged</name>
  <files>backend/sources/views.py, backend/validation/views.py, backend/sources/services.py, backend/validation/services.py</files>
  <read_first>backend/sources/views.py, backend/validation/views.py, common/responses.py, accounts/permissions.py, backend/sources/services.py, backend/validation/services.py</read_first>
  <action>Update the source and validation Django views so GET and POST handlers delegate to the new service-layer entrypoints while preserving existing safe error codes, auth behavior, and success envelope shapes. Keep `_guard_request`, `_authorize_mutation`, and response helpers intact unless a small extraction is needed to remove duplication. Ensure source detail, chunk detail, citation detail, dashboard, validation list/detail, and validation launch still return the same endpoint-level contracts expected by the frontend tests.</action>
  <verify>pnpm backend:test</verify>
  <acceptance_criteria>
    - backend/sources/views.py imports from backend/sources/services.py or sources.services rather than calling every repository function directly
    - backend/validation/views.py imports from backend/validation/services.py or validation.services rather than calling repository functions directly
    - backend/sources/views.py still contains the existing safe error codes for missing sources, chunks, and citations
    - backend/validation/views.py still contains the existing safe error codes for missing validation sets or runs
  </acceptance_criteria>
  <done>The protected Django boundary becomes thinner while maintaining the current client-facing contract.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen targeted Django integration coverage for stewardship and validation seams</name>
  <files>backend/tests/test_admin_stewardship.py, backend/tests/test_admin_validation.py</files>
  <read_first>backend/tests/test_admin_stewardship.py, backend/tests/test_admin_validation.py, backend/tests/test_admin_auth.py, backend/sources/views.py, backend/validation/views.py</read_first>
  <action>Update the existing Django integration suites to assert the service-backed contracts after the seam split. Keep or add tests that prove source dashboard safety, source-detail history groups, source inspection fallbacks, validation set listing, immutable run launch/detail behavior, fallback when the validation store is missing, and auth-safe error envelopes. Add assertions where useful to ensure the view layer delegates to services without changing the envelope shape or status code behavior relied on by the browser.</action>
  <verify>pnpm backend:test && pnpm backend:check</verify>
  <acceptance_criteria>
    - backend/tests/test_admin_stewardship.py still covers dashboard, source detail, inspection, lifecycle, and safe error cases after the service extraction
    - backend/tests/test_admin_validation.py still covers launch, detail, duplicate in-flight protection, missing-store fallback, and safe error cases after the service extraction
    - no public learner route or auth expectations in backend/tests/test_admin_auth.py are broken by the refactor
  </acceptance_criteria>
  <done>The new backend seams are protected by focused Django tests rather than by hope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm backend:test` passes
- [ ] `pnpm backend:check` passes
- [ ] `pnpm backend:lint` passes
- [ ] `pnpm backend:typecheck` passes
- [ ] Source and validation views delegate to services while preserving safe envelopes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Repositories stay narrow and services own readiness-oriented shaping
- Backend stewardship and validation seams are easier to extend without changing the protected maintainer contract
  </success_criteria>

<output>
After completion, create `.planning/phases/03-maintainer-readiness-hardening/03-03-SUMMARY.md`
</output>
